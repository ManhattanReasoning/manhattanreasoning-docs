{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Home","text":"Early prototype \u00b7 v0.1 Cloud FPGA infrastructurefor hardware-reasoning agents <p>Write a hardware design in Python, run it on a real remote FPGA, and read back results \u2014 in a few lines of code.</p> Quickstart \u2192 Python SDK <p>Open-source FPGA toolchains in the cloud \u2014 fast, verifiable reward loops for reinforcement learning on hardware reasoning. Manhattan Reasoning runs a pool of 10 Lattice ECP5 FPGAs behind a single API: submit an Amaranth HDL design, the service builds it (Yosys \u2192 nextpnr-ecp5 \u2192 ecppack) and programs it onto a board, and you run memory-mapped transactions against the live hardware.</p> <p>Early prototype</p> <p>The infrastructure is under active development. Endpoints, payloads, and the SDK surface may change. See Troubleshooting for current known issues.</p>"},{"location":"#the-fast-path-the-sdk","title":"The fast path: the SDK","text":"<p>The <code>cloud_fpga</code> Python package wraps the whole lifecycle \u2014 build, program, register I/O, release \u2014 behind a small decorator API. No manual job polling.</p> <pre><code>import cloud_fpga\n\nclass Regs(cloud_fpga.RegisterMap):\n    DATA_IN  = 0x0004\n    DATA_OUT = 0x0008\n\napp = cloud_fpga.App(\n    \"my_design\",\n    design=\"design.py\",\n    fpga_id=0,\n    registers=Regs,\n    api_key=cloud_fpga.secret(\"CLOUD_FPGA_API_KEY\"),\n)\n\n@app.local_entrypoint()\ndef main():\n    with app:                       # programs the FPGA, releases on exit\n        app.write(Regs.DATA_IN, 0xDEADBEEF)\n        print(hex(app.read(Regs.DATA_OUT)))\n</code></pre> <pre><code>cloud-fpga run my_design.py\n</code></pre>"},{"location":"#start-here","title":"Start here","text":"<ul> <li> <p> Quickstart</p> <p>Install the SDK and run the SAT solver on real hardware in five minutes.</p> </li> <li> <p> Python SDK</p> <p><code>App</code>, register maps, <code>read</code>/<code>write</code>, entrypoints, and sessions.</p> </li> <li> <p> CLI</p> <p><code>cloud-fpga run / status / read / write / reset</code> and friends.</p> </li> <li> <p> Examples</p> <p>A SAT solver, a BERT feed-forward layer, and a minimal smoke test.</p> </li> <li> <p> Architecture</p> <p>How sessions, jobs, and the FPGA state machine fit together.</p> </li> <li> <p> REST API</p> <p>The lower-level HTTP contract the SDK is built on.</p> </li> </ul>"},{"location":"#how-it-works","title":"How it works","text":"<pre><code>flowchart LR\n    A[Your Python&lt;br&gt;cloud_fpga.App] --&gt;|HTTPS| B[Orchestrator&lt;br&gt;build \u00b7 program \u00b7 run]\n    B --&gt;|Wishbone over the wire| C[ECP5 FPGA&lt;br&gt;your design]\n    C --&gt;|results| B --&gt; A</code></pre> <ol> <li>Submit an Amaranth design \u2014 the orchestrator builds and programs it onto an    idle FPGA.</li> <li>Run Wishbone read/write transactions against your live design's registers.</li> <li>Release the board back to idle when you're done.</li> </ol> <p>The SDK does all three for you; the REST API exposes each step directly if you'd rather drive it yourself.</p>"},{"location":"api/rest/","title":"REST API","text":"<p>The orchestrator exposes a REST API over HTTP. All hardware-mutating endpoints are asynchronous: they return <code>202 Accepted</code> with a <code>job_id</code> you poll for progress.</p> <p>Interactive reference</p> <p>When running the orchestrator locally, FastAPI serves a live Swagger UI at <code>/docs</code> and the raw schema at <code>/openapi.json</code> \u2014 always exactly in sync with the deployed build.</p>"},{"location":"api/rest/#conventions","title":"Conventions","text":"<ul> <li><code>fpga_id</code> is an integer <code>0</code>\u2013<code>9</code>. Out-of-range values return <code>422</code>.</li> <li>Headers \u2014 <code>X-API-Key</code> (caller identity) and <code>X-Session-ID</code> (active   session) are read on every route but not yet enforced in the prototype.</li> <li>Errors are returned by FastAPI nested under a <code>detail</code> key:</li> </ul> <pre><code>{ \"detail\": { \"error\": \"fpga_not_idle\", \"message\": \"FPGA 3 is queued, not idle.\" } }\n</code></pre> <p>The inner object follows the <code>ErrorResponse</code> model (<code>error</code> code +   human-readable <code>message</code>).</p>"},{"location":"api/rest/#health","title":"Health","text":""},{"location":"api/rest/#get-health","title":"<code>GET /health</code>","text":"<p>Returns service health and Redis connectivity.</p> <pre><code>{ \"status\": \"ok\", \"redis\": true }\n</code></pre>"},{"location":"api/rest/#fpgas","title":"FPGAs","text":""},{"location":"api/rest/#get-fpga","title":"<code>GET /fpga</code>","text":"<p>List the state and session info for all 10 FPGA nodes. Returns an array of <code>FPGASummary</code>.</p>"},{"location":"api/rest/#get-fpgafpga_id","title":"<code>GET /fpga/{fpga_id}</code>","text":"<p>State snapshot for a single FPGA. An out-of-range <code>fpga_id</code> returns a <code>422</code> validation error.</p> <pre><code>{ \"fpga_id\": 3, \"state\": \"idle\", \"session\": null, \"current_job_id\": null }\n</code></pre>"},{"location":"api/rest/#post-fpgafpga_idsubmit","title":"<code>POST /fpga/{fpga_id}/submit</code>","text":"<p>Upload an HDL source file (multipart form field <code>file</code>) and enqueue a <code>build_and_program</code> job. The FPGA must be <code>idle</code>.</p> <ul> <li>Success: <code>202</code> \u2192 <code>{ \"job_id\", \"fpga_id\", \"status\": \"queued\" }</code>; FPGA \u2192 <code>queued</code>.</li> <li><code>409 fpga_not_idle</code> if the board is not idle.</li> </ul> <pre><code>curl -X POST $BASE/fpga/3/submit -H \"X-API-Key: $KEY\" -F \"file=@design.py\"\n</code></pre>"},{"location":"api/rest/#post-fpgafpga_idrun","title":"<code>POST /fpga/{fpga_id}/run</code>","text":"<p>Enqueue a Wishbone transaction. The FPGA must be <code>reserved</code>.</p> <p>Request body (<code>RunRequest</code>):</p> Field Type Notes <code>op</code> int Wishbone opcode: <code>1</code> = write, <code>2</code> = read <code>address</code> int Wishbone byte address <code>data</code> list[int] 32-bit data words (write only) <ul> <li>Success: <code>202</code> \u2192 <code>{ \"job_id\", \"fpga_id\", \"status\": \"queued\" }</code>.</li> <li><code>409 fpga_not_reserved</code> if the board is not reserved.</li> </ul>"},{"location":"api/rest/#post-fpgafpga_idreset","title":"<code>POST /fpga/{fpga_id}/reset</code>","text":"<p>Enqueue a reset job that reflashes the base LiteX SoC and returns the FPGA to <code>idle</code>. Allowed from <code>reserved</code> or <code>error</code>.</p> <ul> <li>Success: <code>202</code>.</li> <li><code>409 fpga_not_resettable</code> otherwise.</li> </ul>"},{"location":"api/rest/#jobs","title":"Jobs","text":"<p>Paths are prefixed with <code>/fpga/{fpga_id}/jobs</code>.</p>"},{"location":"api/rest/#get-fpgafpga_idjobsjob_id","title":"<code>GET /fpga/{fpga_id}/jobs/{job_id}</code>","text":"<p>Current status and metadata for a job (<code>JobResponse</code>).</p> <pre><code>{\n  \"job_id\": \"\u2026\", \"fpga_id\": 3, \"type\": \"build_and_program\",\n  \"status\": \"queued\", \"created_at\": \"\u2026\", \"updated_at\": \"\u2026\"\n}\n</code></pre> <p><code>status</code> is one of <code>queued</code>, <code>running</code>, <code>complete</code>, <code>failed</code>, <code>cancelled</code>.</p>"},{"location":"api/rest/#get-fpgafpga_idjobsjob_idlogs","title":"<code>GET /fpga/{fpga_id}/jobs/{job_id}/logs</code>","text":"<p>Full build log for the job, as plain text.</p>"},{"location":"api/rest/#get-fpgafpga_idjobsjob_idresult","title":"<code>GET /fpga/{fpga_id}/jobs/{job_id}/result</code>","text":"<p>Wishbone response data from a completed <code>run</code> job.</p> <ul> <li>Success: <code>{ \"ok\": true, \"data\": [ \u2026 ] }</code>.</li> <li><code>409 job_not_complete</code> if the job has not finished.</li> </ul>"},{"location":"api/rest/#delete-fpgafpga_idjobsjob_id","title":"<code>DELETE /fpga/{fpga_id}/jobs/{job_id}</code>","text":"<p>Cancel a queued job before a worker picks it up.</p> <ul> <li>Success: <code>204</code>; job \u2192 <code>cancelled</code>.</li> <li><code>409 job_not_cancellable</code> if it has already started.</li> </ul>"},{"location":"api/rest/#sessions","title":"Sessions","text":""},{"location":"api/rest/#get-fpgafpga_idsession","title":"<code>GET /fpga/{fpga_id}/session</code>","text":"<p>Active session for an FPGA (<code>SessionResponse</code>), including <code>expires_at</code>. <code>404 no_active_session</code> if none.</p>"},{"location":"api/rest/#post-fpgafpga_idsessionrelease","title":"<code>POST /fpga/{fpga_id}/session/release</code>","text":"<p>Release the session by enqueuing a reset job; the FPGA returns to <code>idle</code> once it completes. <code>404 no_active_session</code> if there is no session.</p> <p>For the request/response model definitions in Python, see the Python package reference.</p>"},{"location":"concepts/architecture/","title":"Architecture","text":"<p>The orchestrator manages a fixed pool of 10 FPGA nodes (<code>fpga_id</code> <code>0</code>\u2013<code>9</code>). Every interaction flows through three core concepts: the FPGA state machine, sessions, and jobs.</p>"},{"location":"concepts/architecture/#fpga-state-machine","title":"FPGA state machine","text":"<p>Each FPGA is always in exactly one state. Operations are only valid from certain states, which keeps two users from clobbering the same board.</p> <pre><code>stateDiagram-v2\n    [*] --&gt; idle\n    idle --&gt; queued: submit\n    queued --&gt; building: worker picks up\n    queued --&gt; idle: cancel\n    building --&gt; programming: build ok\n    building --&gt; idle: build failed\n    programming --&gt; reserved: programmed\n    programming --&gt; error: program failed\n    reserved --&gt; reserved: run\n    reserved --&gt; idle: reset / release\n    reserved --&gt; error\n    error --&gt; idle: reset</code></pre> State Meaning <code>idle</code> Free. Accepts a new <code>submit</code>. <code>queued</code> A build-and-program job is waiting for a worker. <code>building</code> The design is compiling in the build sandbox. <code>programming</code> The bitstream is being flashed onto the board. <code>reserved</code> Programmed and held for the submitter. Accepts <code>run</code> transactions. <code>error</code> A program/run step failed. Must be <code>reset</code> back to <code>idle</code>."},{"location":"concepts/architecture/#sessions","title":"Sessions","text":"<p>When a design is successfully programmed, the FPGA becomes <code>reserved</code> and a session is created for the submitter. The session ties a board to its owner and carries a TTL (<code>expires_at</code>). While reserved, only the session holder may run transactions or reset the board.</p> <p>Releasing a session (<code>POST /fpga/{id}/session/release</code>) enqueues a reset job that returns the FPGA to <code>idle</code> once it completes.</p> <p>Reset is unreliable in the prototype</p> <p>The reset job reflashes the base LiteX SoC, which currently fails and can leave the board in <code>error</code> instead of <code>idle</code>. See Troubleshooting for the recovery procedure.</p>"},{"location":"concepts/architecture/#jobs","title":"Jobs","text":"<p>All work is asynchronous. An endpoint that changes hardware state returns <code>202 Accepted</code> with a <code>job_id</code>; you poll the job to follow its progress.</p> <p>There are three job types:</p> Type Triggered by Does <code>build_and_program</code> <code>POST /fpga/{id}/submit</code> Compiles the HDL and flashes the bitstream. <code>run</code> <code>POST /fpga/{id}/run</code> Executes a Wishbone read/write transaction. <code>reset</code> <code>reset</code> / <code>session/release</code> Reflashes the base LiteX SoC, returns to idle. <p>And five job statuses: <code>queued</code> \u2192 <code>running</code> \u2192 <code>complete</code>, or <code>failed</code>, or <code>cancelled</code> (only a <code>queued</code> job can be cancelled).</p> <p>Poll a job with <code>GET /fpga/{id}/jobs/{job_id}</code>; fetch its build log at <code>/logs</code> and, for <code>run</code> jobs, its data at <code>/result</code>.</p>"},{"location":"concepts/architecture/#putting-it-together","title":"Putting it together","text":"<pre><code>sequenceDiagram\n    participant C as Client\n    participant O as Orchestrator\n    participant W as Worker / FPGA\n    C-&gt;&gt;O: POST /fpga/3/submit (design.py)\n    O--&gt;&gt;C: 202 { job_id, status: queued }\n    O-&gt;&gt;W: build_and_program\n    W--&gt;&gt;O: programmed \u2192 reserved\n    C-&gt;&gt;O: GET /fpga/3/jobs/{job_id} (poll)\n    O--&gt;&gt;C: { status: complete }\n    C-&gt;&gt;O: POST /fpga/3/run { op, address, data }\n    O--&gt;&gt;C: 202 { job_id }\n    C-&gt;&gt;O: GET /fpga/3/jobs/{job_id}/result\n    O--&gt;&gt;C: { ok: true, data: [...] }\n    C-&gt;&gt;O: POST /fpga/3/session/release</code></pre>"},{"location":"examples/","title":"Examples","text":"<p>Runnable apps that ship in the <code>Cloud_FPGA</code> repo under <code>examples/</code>. Each is a complete <code>cloud_fpga.App</code> you launch with <code>cloud-fpga run</code>.</p> <ul> <li> <p> SAT solver</p> <p>The flagship workload \u2014 a brute-force boolean satisfiability solver in hardware. Includes a generalized DIMACS runner and a 30-variable variant.</p> </li> <li> <p> BERT feed-forward</p> <p>A real transformer encoder block whose feed-forward matmuls run on the FPGA via an int8 4\u00d74 matmul core.</p> </li> <li> <p> Hello Wishbone</p> <p>The minimal smoke test \u2014 write a pattern to an echo RAM and read it back.</p> </li> </ul> <p>Before you run</p> <p>Install the SDK, set <code>CLOUD_FPGA_API_KEY</code>, and confirm a board is <code>idle</code> (<code>cloud-fpga status</code>). See the Quickstart.</p> <p>Boards land in <code>error</code> after a run</p> <p>These examples release their session on exit, which currently strands the board in <code>error</code>. Recover with the Redis flush in Troubleshooting.</p>"},{"location":"examples/bert-ffn/","title":"BERT feed-forward","text":"<p>An architecturally faithful BERT encoder block whose feed-forward network runs on the FPGA. The pipeline is a real transformer:</p> <pre><code>text \u2192 WordPiece tokenizer \u2192 embeddings \u2192 multi-head self-attention\n     \u2192 FEED-FORWARD (on the FPGA) \u2192 residual + LayerNorm \u2192 block output\n</code></pre> <p>The feed-forward network is the classic BERT FFN \u2014 <code>GELU(x\u00b7W\u2081 + b\u2081)\u00b7W\u2082 + b\u2082</code> with the 4\u00d7 hidden expansion \u2014 and its two matrix multiplications execute on the FPGA using an int8 4\u00d74 matmul core. Everything else (attention, GELU, LayerNorm, softmax) runs in float NumPy on the host.</p> <p>Source: <code>examples/bert_ffn/</code> in the <code>Cloud_FPGA</code> repo.</p>"},{"location":"examples/bert-ffn/#how-the-ffn-reaches-the-fpga","title":"How the FFN reaches the FPGA","text":"<ol> <li>Quantize activations <code>x</code> and weights <code>W</code> to per-tensor int8 (symmetric).</li> <li>Tile the int matmul <code>[M,K]\u00b7[K,N]</code> into 4\u00d74 blocks, accumulating partial    products across K host-side in int64. All-zero tiles are skipped.</li> <li>Run each block on the FPGA: burst-write the two operands, pulse <code>start</code>,    poll <code>done</code>, burst-read the 4\u00d74 result.</li> <li>Dequantize with the product of the two scales.</li> </ol> <p>Every 4\u00d74 block is a network round trip, so the demo dimensions are kept tiny (<code>d_model=8</code>, <code>d_ff=32</code>, <code>seq\u22644</code>) \u2014 about 32 tiles / ~60 s. The tiling engine handles arbitrary sizes; the dimensions are bounded by patience, not correctness.</p>"},{"location":"examples/bert-ffn/#run-it","title":"Run it","text":"<pre><code># hardware-free smoke test (NumPy backend, real tokenizer)\npython examples/bert_ffn/client_sdk.py --sim --text \"fpga runs bert\"\n\n# on hardware (needs: pip install numpy transformers, and CLOUD_FPGA_API_KEY set)\ncloud-fpga run examples/bert_ffn/client_sdk.py\n</code></pre> <pre><code>  input    : 'fpga runs bert'\n  tokens   : ['[CLS]', 'f', '##pg', '##a']\n  config   : d_model=8  d_ff=32  heads=2  seq=4\n  feed-forward backend: FPGA bert_ffn (fpga0)\n  done: 32 tiles run, 0 skipped (zero), 63.8s\n  ...\n  FPGA vs host-int matmul : max abs diff = 0.000e+00 (EXACT MATCH)\n  int8 FFN vs exact float : relative L2 error = 0.838%\n</code></pre>"},{"location":"examples/bert-ffn/#verification","title":"Verification","text":"<p>The run cross-checks the hardware three ways: the FPGA result must be bit-exact against the same int matmul done on the host (proving the hardware is correct), and the int8 FFN is compared against an exact-float forward to report the quantization error (typically &lt;1%).</p>"},{"location":"examples/bert-ffn/#contents","title":"Contents","text":"<ul> <li><code>client_sdk.py</code> \u2014 the SDK app + <code>@local_entrypoint</code>. Reuses the <code>matrix_mult</code>   design unchanged as the FPGA design.</li> <li><code>model.py</code> \u2014 the BERT block in NumPy (embeddings, attention, FFN, LayerNorm)   and int8 quantization helpers.</li> <li><code>fpga_matmul.py</code> \u2014 the 4\u00d74 tiling engine; FPGA and NumPy tile backends.</li> <li><code>tests/unit/</code> \u2014 pure-Python checks of tiling, quantization, and the register map.</li> </ul> <p>See <code>examples/bert_ffn/README.md</code> for full details.</p>"},{"location":"examples/hello-wishbone/","title":"Hello Wishbone","text":"<p>The minimal smoke test \u2014 the \"hello world\" of the platform. The design exposes a 512-word (2 KB) echo RAM over Wishbone; the app writes a pattern and reads it back, confirming the full path (build \u2192 program \u2192 write \u2192 read) works end to end.</p> <p>Source: <code>examples/hello_wishbone/</code> in the <code>Cloud_FPGA</code> repo.</p>"},{"location":"examples/hello-wishbone/#run-it","title":"Run it","text":"<pre><code>cloud-fpga run examples/hello_wishbone/client_sdk.py\n</code></pre> <pre><code>writing pattern ...\nreading back ...\n  [0] 0xdeadbeef \u2192 OK\n  [1] 0xcafebabe \u2192 OK\n  [2] 0x12345678 \u2192 OK\n  [3] 0xabcdef01 \u2192 OK\n</code></pre>"},{"location":"examples/hello-wishbone/#the-whole-app","title":"The whole app","text":"<p>It's about as small as a <code>cloud_fpga.App</code> gets:</p> <pre><code>import cloud_fpga\n\nclass Regs(cloud_fpga.RegisterMap):\n    ECHO = 0x0000          # 512-word echo RAM, byte address 0\n\napp = cloud_fpga.App(\n    \"hello_wishbone\",\n    design=\"examples/hello_wishbone/design.py\",\n    fpga_id=0,\n    registers=Regs,\n    api_key=cloud_fpga.secret(\"CLOUD_FPGA_API_KEY\"),\n)\n\n@app.local_entrypoint()\ndef main():\n    with app:\n        pattern = [0xDEADBEEF, 0xCAFEBABE, 0x12345678, 0xABCDEF01]\n        for i, word in enumerate(pattern):\n            app.write(Regs.ECHO + i * 4, word)\n        for i, expected in enumerate(pattern):\n            got = app.read(Regs.ECHO + i * 4)\n            status = \"OK\" if got == expected else f\"MISMATCH (got {got:#010x})\"\n            print(f\"  [{i}] {expected:#010x} \u2192 {status}\")\n</code></pre> <p>This is the best starting point for writing your own app: swap in your own design and register map, and replace <code>main()</code> with your logic.</p>"},{"location":"examples/sat-solver/","title":"SAT solver","text":"<p>The flagship workload: a brute-force boolean satisfiability solver in hardware. Every clock cycle it evaluates all clauses combinationally in parallel against the current candidate assignment, while a binary counter sweeps the assignment space. Worst case is <code>2^n_vars</code> cycles \u2014 \u224820 \u00b5s for 10 variables at 50 MHz.</p> <p>It's deliberately the naive baseline: a fast, verifiable reward signal that an agent is meant to improve on (toward DPLL/CDCL-style reasoning).</p> <p>Source: <code>examples/sat_solver/</code> in the <code>Cloud_FPGA</code> repo.</p>"},{"location":"examples/sat-solver/#run-it","title":"Run it","text":"<pre><code>cloud-fpga run examples/sat_solver/client_sdk.py\n</code></pre> <pre><code>[cloud-fpga] programming 'sat_solver' onto FPGA 0 ...\n[cloud-fpga] done.\nsolving ...\nSAT  (8 cycles)  model={'x1': True, 'x2': True, 'x3': True}\nsolving (x1) \u2227 (\u00acx1) ...\nUNSAT  (3 cycles)\n</code></pre> <p><code>client_sdk.py</code> solves a built-in SAT formula and a built-in UNSAT formula.</p>"},{"location":"examples/sat-solver/#solve-your-own-formula","title":"Solve your own formula","text":"<p>A generalized runner (<code>solve.py</code>) accepts a DIMACS formula \u2014 variables are 1-based, a negative number means negated, <code>0</code> ends a clause:</p> <pre><code># inline DIMACS\nSAT_FORMULA=\"1 2 3 0 -1 2 0 -2 3 0 1 -3 0\" \\\n  cloud-fpga run examples/sat_solver/solve.py\n\n# from a .cnf file\nSAT_CNF=myproblem.cnf cloud-fpga run examples/sat_solver/solve.py\n\n# pick a different board\nFPGA_ID=1 SAT_CNF=myproblem.cnf cloud-fpga run examples/sat_solver/solve.py\n</code></pre>"},{"location":"examples/sat-solver/#limits","title":"Limits","text":"<p>The default design is sized at synthesis time:</p> Parameter Max Constant Variables 10 <code>MAX_VARS</code> Clauses 20 <code>MAX_CLAUSES</code> Literals per clause 10 <code>CLAUSE_LEN</code> <p>The variable index is packed into a 4-bit field, so 16 variables is the ceiling without changing the literal encoding. Beyond that, brute force itself is the wall: the search is <code>2^n</code>, so it gets impractical around 25\u201330 variables.</p>"},{"location":"examples/sat-solver/#register-map","title":"Register map","text":"<p>32-bit registers; byte offset = 4 \u00d7 word offset, relative to the design's Wishbone region.</p> Word Byte Access Contents 0 <code>0x000</code> W bit 0 = start (auto-clears) 0 <code>0x000</code> R bit 0 = done, bit 1 = sat 1 <code>0x004</code> W <code>n_vars</code> 2 <code>0x008</code> W <code>n_clauses</code> 3 <code>0x00C</code> R model \u2014 bit i = value of variable i+1 (valid when sat) 4 <code>0x010</code> R cycles taken (diagnostic) 8 + c\u00b710 + l <code>0x020 + \u2026</code> W literal for clause c, slot l: bits[3:0]=variable (0-based), bit 4=negated, bit 5=used <p>Solve sequence: write the literal block \u2192 write <code>n_vars</code>/<code>n_clauses</code> \u2192 write start \u2192 poll word 0 until <code>done</code> \u2192 if <code>sat</code>, read the model from word 3.</p>"},{"location":"examples/sat-solver/#going-bigger-a-30-variable-variant","title":"Going bigger: a 30-variable variant","text":"<p>To watch brute force stretch its legs, a wide variant widens the literal field to 5 bits (so up to 30 variables) and the cycle counter to 32 bits (so it doesn't overflow past 2\u00b2\u2070). A full 2\u00b3\u2070 sweep runs in real time on the FPGA:</p> <pre><code># (x30) \u2227 (\u00acx30): UNSAT, but references x30 so it sweeps all 2^30 assignments\nSAT_FORMULA=\"30 0 -30 0\" cloud-fpga run examples/sat_solver/solve_wide.py\n</code></pre> <pre><code>formula: 30 vars, 2 clauses  (search space = 2^30)\nsolving on the FPGA ...\nUNSAT  (1,073,741,824 cycles, 21.47s on-chip @50MHz, 22.1s wall)\n</code></pre> <p>Exactly 2\u00b3\u2070 cycles at 50 MHz \u2248 21.5 s of pure hardware search, with the network adding well under a second.</p>"},{"location":"examples/sat-solver/#without-hardware","title":"Without hardware","text":"<p>The example also has pure-Python and Amaranth simulation tests, plus a reference client (<code>client.py</code>) that speaks the wire protocol directly to a board on your LAN. See <code>examples/sat_solver/README.md</code>.</p>"},{"location":"guides/cli/","title":"The CLI","text":"<p><code>cloud-fpga</code> is installed with the SDK. It programs FPGAs, inspects cluster and job state, and pokes registers on a live board.</p> <pre><code>cloud-fpga --help\ncloud-fpga &lt;command&gt; [options]\n</code></pre> <p>Global options (accepted by every command):</p> Option Default Purpose <code>--api-key KEY</code> <code>$CLOUD_FPGA_API_KEY</code> API key. <code>--api-url URL</code> <code>https://api.manhattanreasoning.com</code> Orchestrator base URL."},{"location":"guides/cli/#run-program-and-run-an-app","title":"<code>run</code> \u2014 program and run an app","text":"<pre><code>cloud-fpga run &lt;file.py&gt; [--fpga-id N] [--no-program]\n</code></pre> <p>Loads the <code>cloud_fpga.App</code> from <code>file.py</code>, programs the FPGA, then calls its <code>@local_entrypoint</code>.</p> Option Purpose <code>--fpga-id N</code> Override the <code>fpga_id</code> set in the file. <code>--no-program</code> Skip programming (the board already has this design loaded). <pre><code>cloud-fpga run examples/sat_solver/client_sdk.py\ncloud-fpga run my_design.py --fpga-id 3\n</code></pre>"},{"location":"guides/cli/#status-show-fpga-states","title":"<code>status</code> \u2014 show FPGA states","text":"<pre><code>cloud-fpga status [fpga_id]\n</code></pre> <p>With no argument, prints the full table; with an <code>fpga_id</code>, shows that board's detail (state, owner, current job, session).</p> <pre><code>  ID  STATE           OWNER         CURRENT JOB\n  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n   0  idle            -             -\n   3  reserved        dev           a1b2c3d4\n</code></pre> <p>States: <code>idle</code>, <code>queued</code>, <code>building</code>, <code>programming</code>, <code>reserved</code>, <code>error</code> \u2014 see Architecture.</p>"},{"location":"guides/cli/#read-read-register-words","title":"<code>read</code> \u2014 read register words","text":"<pre><code>cloud-fpga read &lt;fpga_id&gt; &lt;address&gt; [--count N]\n</code></pre> <p><code>address</code> is a byte address, hex or decimal. Reads <code>--count</code> words (default 1) from a live (reserved) board.</p> <pre><code>cloud-fpga read 0 0x10            #   0x10: 0x00000005\ncloud-fpga read 0 0x20 --count 4\n</code></pre>"},{"location":"guides/cli/#write-write-a-register-word","title":"<code>write</code> \u2014 write a register word","text":"<pre><code>cloud-fpga write &lt;fpga_id&gt; &lt;address&gt; &lt;value&gt;\n</code></pre> <p>Both <code>address</code> and <code>value</code> accept hex or decimal.</p> <pre><code>cloud-fpga write 0 0x04 3\ncloud-fpga write 0 0x00 0x1\n</code></pre>"},{"location":"guides/cli/#reset-return-a-board-to-idle","title":"<code>reset</code> \u2014 return a board to idle","text":"<pre><code>cloud-fpga reset &lt;fpga_id&gt;\n</code></pre> <p>Reflashes the base SoC and returns the board to <code>idle</code>.</p> <p>Reset can fail in the prototype</p> <p>The base-SoC reflash currently fails and can leave the board in <code>error</code>. If <code>reset</code> doesn't recover a board, use the Redis flush in Troubleshooting.</p>"},{"location":"guides/cli/#job-logs-cancel-job-control","title":"<code>job</code>, <code>logs</code>, <code>cancel</code> \u2014 job control","text":"<pre><code>cloud-fpga job    &lt;fpga_id&gt; &lt;job_id&gt;     # status + metadata\ncloud-fpga logs   &lt;fpga_id&gt; &lt;job_id&gt;     # build log (great for build failures)\ncloud-fpga cancel &lt;fpga_id&gt; &lt;job_id&gt;     # cancel a queued job\n</code></pre> <p>When a <code>run</code> build fails, <code>cloud-fpga logs &lt;fpga_id&gt; &lt;job_id&gt;</code> prints the Yosys / nextpnr output so you can see what the synthesizer rejected.</p>"},{"location":"guides/cli/#see-also","title":"See also","text":"<ul> <li>Python SDK \u2014 the <code>App</code> / <code>RegisterMap</code> API the CLI drives.</li> <li>REST API \u2014 the HTTP endpoints behind each command.</li> </ul>"},{"location":"guides/quickstart/","title":"Quickstart","text":"<p>Install the SDK, program an FPGA, and run a real workload \u2014 the brute-force SAT solver \u2014 on hardware, in about five minutes.</p> <p>What you need</p> <ul> <li>Python 3.12 or 3.13. (Python 3.14 currently ships a broken <code>ensurepip</code>   on Homebrew, so <code>python -m venv</code> fails \u2014 use 3.12/3.13.)</li> <li>A <code>CLOUD_FPGA_API_KEY</code>. Auth is not enforced in the prototype, so any   non-empty value works for now, but the variable must be set.</li> <li>A clone of the <code>Cloud_FPGA</code>   repo (for the SDK and the example designs).</li> </ul>"},{"location":"guides/quickstart/#1-install-the-sdk","title":"1. Install the SDK","text":"<pre><code>cd Cloud_FPGA\n\n# Use 3.12 or 3.13 explicitly if your default python3 is 3.14\npython3.13 -m venv .venv\nsource .venv/bin/activate\n\npip install -e ./sdk\n</code></pre> <p>This installs the <code>cloud_fpga</code> package and the <code>cloud-fpga</code> CLI:</p> <pre><code>cloud-fpga --help\n</code></pre>"},{"location":"guides/quickstart/#2-set-your-api-key","title":"2. Set your API key","text":"<pre><code>export CLOUD_FPGA_API_KEY=dev      # any non-empty value during the prototype\n</code></pre> <p>The SDK targets <code>https://api.manhattanreasoning.com</code> by default. Point it elsewhere with <code>--api-url</code> (CLI) or <code>api_url=</code> (in code).</p>"},{"location":"guides/quickstart/#3-check-the-cluster","title":"3. Check the cluster","text":"<pre><code>cloud-fpga status\n</code></pre> <pre><code>  ID  STATE           OWNER         CURRENT JOB\n  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n   0  idle            -             -\n   1  idle            -             -\n   ...\n</code></pre> <p>Pick any board showing <code>idle</code>. If a board shows <code>error</code>, see Troubleshooting.</p>"},{"location":"guides/quickstart/#4-run-the-sat-solver","title":"4. Run the SAT solver","text":"<p>The <code>sat_solver</code> example ships a ready-to-run SDK app. From the repo root:</p> <pre><code>cloud-fpga run examples/sat_solver/client_sdk.py\n</code></pre> <p>You'll see the FPGA get programmed, then two formulas solved:</p> <pre><code>[cloud-fpga] programming 'sat_solver' onto FPGA 0 ...\n[cloud-fpga] done.\nsolving ...\nSAT  (8 cycles)  model={'x1': True, 'x2': True, 'x3': True}\nsolving (x1) \u2227 (\u00acx1) ...\nUNSAT  (3 cycles)\n</code></pre> <p>The solver evaluates all clauses in parallel each clock cycle while a counter sweeps candidate assignments \u2014 worst case <code>2^n_vars</code> cycles (\u224820 \u00b5s for 10 variables at 50 MHz).</p> <p>It may leave the board in <code>error</code></p> <p>The example runs inside <code>with app:</code>, which releases the session on exit. Releasing reflashes the base SoC, which currently fails and parks the FPGA in <code>error</code>. Recover it with the Redis flush described in Troubleshooting.</p>"},{"location":"guides/quickstart/#5-solve-your-own-formula","title":"5. Solve your own formula","text":"<p>The repo includes a generalized runner that accepts a DIMACS formula via an environment variable:</p> <pre><code># (x1 \u2228 x2 \u2228 x3) \u2227 (\u00acx1 \u2228 x2) \u2227 (\u00acx2 \u2228 x3) \u2227 (x1 \u2228 \u00acx3)\nSAT_FORMULA=\"1 2 3 0 -1 2 0 -2 3 0 1 -3 0\" \\\n  cloud-fpga run examples/sat_solver/solve.py\n\n# from a .cnf file\nSAT_CNF=myproblem.cnf cloud-fpga run examples/sat_solver/solve.py\n</code></pre> <p>Limits for the default design: \u226410 variables, \u226420 clauses, \u226410 literals/clause. See the SAT solver example for the register map and a 30-variable variant.</p>"},{"location":"guides/quickstart/#whats-next","title":"What's next","text":"<ul> <li> The Python SDK \u2014 write your own <code>App</code>.</li> <li> The CLI \u2014 every <code>cloud-fpga</code> command.</li> <li> Architecture \u2014 the state machine behind it all.</li> <li> Troubleshooting \u2014 when a board gets stuck.</li> </ul>"},{"location":"guides/rest/","title":"Using the REST API directly","text":"<p>The Python SDK is the recommended way to use the platform. If you'd rather drive the orchestrator yourself \u2014 from another language, a shell script, or to understand what the SDK does \u2014 this walks the full lifecycle with <code>curl</code>. The same calls work from any HTTP client. See the REST API reference for the complete endpoint contract.</p> <p>Base URL</p> <p>Replace <code>$BASE</code> with your orchestrator URL \u2014 <code>https://api.manhattanreasoning.com</code>, or <code>http://localhost:8000</code> when running locally.</p>"},{"location":"guides/rest/#authentication","title":"Authentication","text":"<p>Two headers are threaded through every request:</p> Header Purpose <code>X-API-Key</code> Identifies the caller (recorded as the job/session owner). <code>X-Session-ID</code> Identifies the active session for a reserved FPGA. <p>Not enforced yet</p> <p>In the current prototype these headers are read but not enforced \u2014 auth will be layered in without changing the route signatures. Send them anyway so your integration is ready.</p>"},{"location":"guides/rest/#1-find-a-free-fpga","title":"1. Find a free FPGA","text":"<pre><code>curl $BASE/fpga\n</code></pre> <p>Each entry reports a <code>state</code>. Pick one that is <code>idle</code>.</p>"},{"location":"guides/rest/#2-submit-a-design","title":"2. Submit a design","text":"<p>Upload an HDL source file to an idle board. This enqueues a <code>build_and_program</code> job and returns <code>202</code> immediately.</p> <pre><code>curl -X POST $BASE/fpga/3/submit \\\n  -H \"X-API-Key: $API_KEY\" \\\n  -F \"file=@design.py\"\n</code></pre> <pre><code>{ \"job_id\": \"a1b2c3d4-...\", \"fpga_id\": 3, \"status\": \"queued\" }\n</code></pre>"},{"location":"guides/rest/#3-poll-the-job","title":"3. Poll the job","text":"<p>The board moves <code>queued \u2192 building \u2192 programming \u2192 reserved</code>. Poll the job until it is <code>complete</code>:</p> <pre><code>curl $BASE/fpga/3/jobs/a1b2c3d4-...\n</code></pre> <p>Stream the build log at any point:</p> <pre><code>curl $BASE/fpga/3/jobs/a1b2c3d4-.../logs\n</code></pre> <p>When the job completes, the FPGA is <code>reserved</code> for you.</p>"},{"location":"guides/rest/#4-run-a-transaction","title":"4. Run a transaction","text":"<p>Issue a Wishbone read/write against the programmed design. <code>op</code> is <code>1</code> for write and <code>2</code> for read.</p> <pre><code>curl -X POST $BASE/fpga/3/run \\\n  -H \"X-API-Key: $API_KEY\" \\\n  -H \"X-Session-ID: $SESSION_ID\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{ \"op\": 1, \"address\": 0, \"data\": [42] }'\n</code></pre> <p>This returns a <code>job_id</code>; fetch the result once it completes:</p> <pre><code>curl $BASE/fpga/3/jobs/&lt;run_job_id&gt;/result\n</code></pre> <pre><code>{ \"ok\": true, \"data\": [42] }\n</code></pre>"},{"location":"guides/rest/#5-release-the-board","title":"5. Release the board","text":"<p>Return the FPGA to <code>idle</code> so others can use it:</p> <pre><code>curl -X POST $BASE/fpga/3/session/release \\\n  -H \"X-API-Key: $API_KEY\" \\\n  -H \"X-Session-ID: $SESSION_ID\"\n</code></pre>"},{"location":"guides/rest/#run-the-orchestrator-locally","title":"Run the orchestrator locally","text":"<p>The orchestrator is a FastAPI app backed by Redis.</p> <p>Prerequisites: Python \u2265 3.12 (use 3.12/3.13; see Troubleshooting) and a running Redis (Docker is the easiest way to get one).</p> <pre><code># From the repo root \u2014 use a virtualenv (system/conda Python may refuse\n# `pip install` with an \"externally-managed-environment\" error).\npython3.13 -m venv .venv\nsource .venv/bin/activate\n\npip install -e \"orchestrator[dev]\"\n\n# Start Redis (defaults match REDIS_URL=redis://localhost:6379)\ndocker run -d -p 6379:6379 redis\n\n# Boot the API\nuvicorn cloud_fpga_orchestrator.api.app:app --reload\n</code></pre> <p>The interactive OpenAPI explorer is then available at <code>http://localhost:8000/docs</code> (Swagger UI) \u2014 handy while developing.</p> <p>What works today vs. what needs hardware</p> <p>The install/boot steps and the list \u2192 submit \u2192 poll \u2192 cancel flow are verified end to end against a local Redis: <code>submit</code> returns <code>202</code> and moves the board to <code>queued</code>, jobs poll correctly, and the documented <code>409</code>/<code>404</code> guards fire as described.</p> <p>The <code>run</code> \u2192 <code>result</code> \u2192 <code>session/release</code> steps require the FPGA to reach the <code>reserved</code> state, which only happens once a worker finishes a build-and-program job \u2014 and that depends on the build toolchain and physical boards being wired up. Until then, those calls return <code>409 fpga_not_reserved</code> by design.</p> <p>Next: read Architecture to understand the state machine, or the full REST API reference.</p>"},{"location":"guides/sdk/","title":"The Python SDK","text":"<p>The <code>cloud_fpga</code> package wraps the full FPGA lifecycle \u2014 build, program, register I/O, and release \u2014 behind a small Modal-style decorator API. It is the recommended way to use the platform; the REST API is the lower-level contract underneath.</p>"},{"location":"guides/sdk/#install","title":"Install","text":"<pre><code>pip install -e ./sdk      # from a clone of the Cloud_FPGA repo\n</code></pre> <p>Requires Python 3.12 or 3.13. Installs the <code>cloud_fpga</code> package and the <code>cloud-fpga</code> CLI.</p>"},{"location":"guides/sdk/#the-mental-model","title":"The mental model","text":"<p>You describe an application with three things:</p> <ol> <li>a design \u2014 an Amaranth <code>.py</code> file whose top-level module is a Wishbone B4    slave (see Architecture);</li> <li>a register map \u2014 the byte offsets your design exposes;</li> <li>API config \u2014 which board (<code>fpga_id</code>) and your key.</li> </ol> <p>Then you read and write registers. The first access programs the FPGA automatically.</p> <pre><code>import cloud_fpga\n\nclass Regs(cloud_fpga.RegisterMap):\n    CTRL     = 0x0000\n    DATA_IN  = 0x0004\n    DATA_OUT = 0x0008\n\napp = cloud_fpga.App(\n    \"my_design\",\n    design=\"design.py\",\n    fpga_id=0,\n    registers=Regs,\n    api_key=cloud_fpga.secret(\"CLOUD_FPGA_API_KEY\"),\n)\n\n@app.local_entrypoint()\ndef main():\n    with app:\n        app.write(Regs.DATA_IN, 0x1234)\n        app.write(Regs.CTRL, 1)            # kick off\n        print(hex(app.read(Regs.DATA_OUT)))\n</code></pre> <p>Run it with the CLI (which calls the entrypoint after programming):</p> <pre><code>cloud-fpga run my_design.py\n</code></pre>"},{"location":"guides/sdk/#app","title":"<code>App</code>","text":"<pre><code>cloud_fpga.App(\n    name,\n    *,\n    design,                 # path to the Amaranth .py design\n    fpga_id,                # which board (0\u20139)\n    registers=None,         # a RegisterMap subclass (optional)\n    api_key=None,           # defaults to $CLOUD_FPGA_API_KEY\n    api_url=\"https://api.manhattanreasoning.com\",\n)\n</code></pre> <p>Creating an <code>App</code> registers it so the CLI can discover it \u2014 you don't export anything. If you define several in one file, <code>cloud-fpga run</code> uses the last one.</p>"},{"location":"guides/sdk/#appreadaddr-count1","title":"<code>app.read(addr, count=1)</code>","text":"<p>Read <code>count</code> 32-bit words starting at byte address <code>addr</code>. Returns a single <code>int</code> when <code>count == 1</code>, otherwise a <code>list[int]</code>. Programs the FPGA first if it hasn't been programmed yet.</p> <pre><code>status = app.read(Regs.CTRL)            # one word -&gt; int\nblock  = app.read(0x0020, count=8)      # eight words -&gt; list[int]\n</code></pre>"},{"location":"guides/sdk/#appwriteaddr-value","title":"<code>app.write(addr, value)</code>","text":"<p>Write one or more 32-bit words to byte address <code>addr</code>. <code>value</code> may be a single <code>int</code> or a <code>list[int]</code> for a burst write. Programs the FPGA first if needed.</p> <pre><code>app.write(Regs.DATA_IN, 0xDEADBEEF)     # single word\napp.write(0x0020, [0] * 200)            # burst (e.g. clear a region)\n</code></pre>"},{"location":"guides/sdk/#apprelease","title":"<code>app.release()</code>","text":"<p>Release the active session, returning the board to <code>idle</code>. Returns the reset <code>job_id</code>.</p> <pre><code>app.release()\n</code></pre>"},{"location":"guides/sdk/#context-manager","title":"Context manager","text":"<p><code>App</code> is a context manager that releases on exit \u2014 the idiomatic way to scope a session:</p> <pre><code>@app.local_entrypoint()\ndef main():\n    with app:\n        app.write(Regs.DATA_IN, 42)\n        result = app.read(Regs.DATA_OUT)\n    # session released here\n</code></pre> <p>Releasing currently strands the board in <code>error</code></p> <p>Releasing reflashes the base SoC, which currently fails and leaves the FPGA in <code>error</code>. Until that's fixed, recover with the Redis flush in Troubleshooting. You can also skip the auto-release by not using <code>with app:</code> and resetting manually later.</p>"},{"location":"guides/sdk/#registermap","title":"<code>RegisterMap</code>","text":"<p>A marker base class for your design's address map. Subclass it and set integer class attributes for the byte offsets:</p> <pre><code>class Regs(cloud_fpga.RegisterMap):\n    CTRL     = 0x0000      # word 0\n    N_VARS   = 0x0004      # word 1\n    MODEL    = 0x000C      # word 3\n</code></pre> <p>Byte offset = 4 \u00d7 word offset. Passing <code>registers=Regs</code> to <code>App</code> is optional (it's just for your own organization \u2014 <code>read</code>/<code>write</code> take raw addresses).</p>"},{"location":"guides/sdk/#applocal_entrypoint","title":"<code>@app.local_entrypoint()</code>","text":"<p>Marks the function the CLI runs after programming. It is not called when you <code>import</code> or <code>python</code> the file \u2014 only by <code>cloud-fpga run</code>. This lets the same file double as an importable module and a runnable app.</p>"},{"location":"guides/sdk/#secretenv_var","title":"<code>secret(env_var)</code>","text":"<p>Reads a required environment variable, raising <code>ValueError</code> immediately (at import time) if it's unset \u2014 so a missing key fails loudly before any work starts.</p> <pre><code>api_key = cloud_fpga.secret(\"CLOUD_FPGA_API_KEY\")\n</code></pre>"},{"location":"guides/sdk/#module-level-functions","title":"Module-level functions","text":"<p>For session management outside an <code>App</code>:</p> <pre><code>cloud_fpga.get_session(fpga_id, api_key, api_url)      # current session info\ncloud_fpga.release_session(fpga_id, api_key, api_url)  # release, returns job_id\n</code></pre>"},{"location":"guides/sdk/#a-complete-example","title":"A complete example","text":"<p>The SAT solver example is a good template \u2014 a <code>RegisterMap</code>, helper encoders, an <code>App</code>, and a <code>@local_entrypoint</code> that writes a formula, starts the solve, polls for completion, and reads back the model.</p>"},{"location":"guides/sdk/#see-also","title":"See also","text":"<ul> <li>CLI reference \u2014 driving apps from the terminal.</li> <li>Examples \u2014 runnable apps.</li> <li>REST API \u2014 what the SDK calls under the hood.</li> </ul>"},{"location":"guides/troubleshooting/","title":"Troubleshooting","text":"<p>Known issues in the current prototype and how to work around them.</p>"},{"location":"guides/troubleshooting/#fpga-stuck-in-error","title":"FPGA stuck in <code>error</code>","text":"<p>Symptom. After a run, <code>cloud-fpga status</code> shows a board in <code>error</code>, and new <code>run</code>/<code>submit</code> calls against it fail.</p> <p>Cause. Releasing a session (<code>app.release()</code>, leaving a <code>with app:</code> block, or <code>cloud-fpga reset</code>) reflashes the base LiteX SoC. That reflash currently fails (a missing <code>base.bit</code> on the orchestrator), stranding the board in <code>error</code>. Because the in-band <code>reset</code> does the same failing reflash, it can't recover the board either.</p> <p>Fix. Flush the orchestrator's Redis state, which returns every board to <code>idle</code>. The <code>Cloud_FPGA</code> repo ships a helper:</p> <pre><code># point it at your key + droplet, then run\nexport FPGA_SSH_KEY=~/.ssh/id_ed25519       # the team's DigitalOcean key\nexport FPGA_DROPLET=root@&lt;orchestrator-ip&gt;\nexamples/bert_ffn/reset_idle.sh\n</code></pre> <p>Or run the one line it boils down to:</p> <pre><code>ssh -i \"$FPGA_SSH_KEY\" root@&lt;orchestrator-ip&gt; \"redis-cli FLUSHALL\"\ncloud-fpga status        # should show all idle again\n</code></pre> <p>Avoid the prompt every time</p> <p>If the SSH key has a passphrase, load it once with <code>ssh-add ~/.ssh/id_ed25519</code> so subsequent calls don't re-prompt. (On macOS, <code>ssh-add</code> can be finicky; if it refuses, you can always pass the key inline with <code>ssh -i</code>.)</p> <p>This affects every <code>with app:</code> run</p> <p>Until the base-SoC reflash is fixed, any example that releases its session (which includes the shipped SAT and BERT examples) will leave its board in <code>error</code>. Expect to flush between runs.</p>"},{"location":"guides/troubleshooting/#python-m-venv-fails-with-an-ensurepip-error","title":"<code>python -m venv</code> fails with an <code>ensurepip</code> error","text":"<p>Symptom.</p> <pre><code>Error: Command '[...python3.14, -m, ensurepip, --upgrade, --default-pip]'\nreturned non-zero exit status 1.\n</code></pre> <p>Cause. Python 3.14 (notably the Homebrew build) ships a broken <code>ensurepip</code>, so venv creation fails.</p> <p>Fix. Use Python 3.12 or 3.13 explicitly:</p> <pre><code>python3.13 -m venv .venv\nsource .venv/bin/activate\npip install -e ./sdk\n</code></pre>"},{"location":"guides/troubleshooting/#submit-jobs-queue-but-never-program","title":"<code>submit</code> jobs queue but never program","text":"<p>Symptom. A submitted design sits in <code>queued</code>/<code>building</code> and never reaches <code>reserved</code>; <code>run</code> returns <code>409 fpga_not_reserved</code>.</p> <p>Cause. The board's worker or the build toolchain isn't running on that orchestrator deployment. The build server needs the full Amaranth \u2192 Yosys \u2192 nextpnr-ecp5 \u2192 ecppack toolchain and a connected host agent.</p> <p>Fix. Use a deployment with workers + toolchain wired up, or check <code>cloud-fpga logs &lt;fpga_id&gt; &lt;job_id&gt;</code> for the failure. See Architecture \u2192 Jobs.</p>"},{"location":"guides/troubleshooting/#secret-raises-valueerror-at-startup","title":"<code>secret(...)</code> raises <code>ValueError</code> at startup","text":"<p><code>cloud_fpga.secret(\"CLOUD_FPGA_API_KEY\")</code> fails fast if the variable is unset. Export it (any non-empty value works during the prototype, since auth isn't enforced):</p> <pre><code>export CLOUD_FPGA_API_KEY=dev\n</code></pre>"},{"location":"guides/troubleshooting/#a-build-failed-where-are-the-logs","title":"A build failed \u2014 where are the logs?","text":"<pre><code>cloud-fpga logs &lt;fpga_id&gt; &lt;job_id&gt;\n</code></pre> <p>This prints the synthesizer output (Yosys / nextpnr-ecp5), which is where most design rejections show up \u2014 unsupported constructs, resource overflows, or an interface that doesn't match the Wishbone B4 slave contract.</p>"},{"location":"reference/orchestrator/","title":"Python package reference","text":"<p>API reference for the <code>cloud_fpga_orchestrator</code> package, generated directly from the source with mkdocstrings. It stays in sync with the code and grows richer as docstrings are added.</p>"},{"location":"reference/orchestrator/#domain-models","title":"Domain models","text":"<p>The core state types shared across the service.</p>"},{"location":"reference/orchestrator/#api-requestresponse-models","title":"API request/response models","text":"<p>The Pydantic models that define the REST contract.</p>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.models.RunRequest","title":"RunRequest","text":"<p>               Bases: <code>BaseModel</code></p> <p>Payload for POST /fpga/{id}/run.</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/models.py</code> <pre><code>class RunRequest(BaseModel):\n    \"\"\"Payload for POST /fpga/{id}/run.\"\"\"\n\n    op: int = Field(description=\"Wishbone opcode: 1 = write, 2 = read\")\n    address: int = Field(description=\"Wishbone byte address\")\n    data: list[int] = Field(\n        default_factory=list, description=\"32-bit data words (write only)\"\n    )\n    count: int = Field(\n        default=1, description=\"number of 32-bit words to read (read only)\"\n    )\n</code></pre>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.models.SessionResponse","title":"SessionResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>Active session information for an FPGA.</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/models.py</code> <pre><code>class SessionResponse(BaseModel):\n    \"\"\"Active session information for an FPGA.\"\"\"\n\n    session_id: UUID\n    fpga_id: int\n    owner: str\n    expires_at: datetime\n</code></pre>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.models.JobResponse","title":"JobResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>Status and metadata for a single job.</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/models.py</code> <pre><code>class JobResponse(BaseModel):\n    \"\"\"Status and metadata for a single job.\"\"\"\n\n    job_id: UUID\n    fpga_id: int\n    type: JobType\n    status: JobStatus\n    created_at: datetime\n    updated_at: datetime\n</code></pre>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.models.FPGASummary","title":"FPGASummary","text":"<p>               Bases: <code>BaseModel</code></p> <p>State snapshot for a single FPGA node.</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/models.py</code> <pre><code>class FPGASummary(BaseModel):\n    \"\"\"State snapshot for a single FPGA node.\"\"\"\n\n    fpga_id: int\n    state: FPGAState\n    session: SessionResponse | None\n    current_job_id: UUID | None\n</code></pre>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.models.SubmitResponse","title":"SubmitResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>Returned on a successful POST /fpga/{id}/submit (HTTP 202).</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/models.py</code> <pre><code>class SubmitResponse(BaseModel):\n    \"\"\"Returned on a successful POST /fpga/{id}/submit (HTTP 202).\"\"\"\n\n    job_id: UUID\n    fpga_id: int\n    status: JobStatus = JobStatus.QUEUED\n</code></pre>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.models.RunResponse","title":"RunResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>Returned on a successful POST /fpga/{id}/run (HTTP 202).</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/models.py</code> <pre><code>class RunResponse(BaseModel):\n    \"\"\"Returned on a successful POST /fpga/{id}/run (HTTP 202).\"\"\"\n\n    job_id: UUID\n    fpga_id: int\n    status: JobStatus = JobStatus.QUEUED\n</code></pre>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.models.RunResultResponse","title":"RunResultResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>Result data available once a run job completes.</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/models.py</code> <pre><code>class RunResultResponse(BaseModel):\n    \"\"\"Result data available once a run job completes.\"\"\"\n\n    ok: bool\n    data: list[int]\n</code></pre>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.models.HealthResponse","title":"HealthResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>System health snapshot.</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/models.py</code> <pre><code>class HealthResponse(BaseModel):\n    \"\"\"System health snapshot.\"\"\"\n\n    status: str\n    redis: bool\n</code></pre>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.models.ErrorResponse","title":"ErrorResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>Standard error envelope returned on all non-2xx responses.</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/models.py</code> <pre><code>class ErrorResponse(BaseModel):\n    \"\"\"Standard error envelope returned on all non-2xx responses.\"\"\"\n\n    error: str = Field(description=\"Machine-readable error code\")\n    message: str = Field(description=\"Human-readable description\")\n    job_id: UUID | None = None\n</code></pre>"},{"location":"reference/orchestrator/#application-factory","title":"Application factory","text":""},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.app.lifespan","title":"lifespan  <code>async</code>","text":"<pre><code>lifespan(app: FastAPI) -&gt; AsyncGenerator[None, None]\n</code></pre> <p>Initialize resources on startup and clean up on shutdown.</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/app.py</code> <pre><code>@asynccontextmanager\nasync def lifespan(app: FastAPI) -&gt; AsyncGenerator[None, None]:\n    \"\"\"Initialize resources on startup and clean up on shutdown.\"\"\"\n    init_pool()\n    yield\n    await close_pool()\n</code></pre>"},{"location":"reference/orchestrator/#cloud_fpga_orchestrator.api.app.create_app","title":"create_app","text":"<pre><code>create_app() -&gt; FastAPI\n</code></pre> <p>Create and configure the FastAPI application.</p> Source code in <code>_code/orchestrator/src/cloud_fpga_orchestrator/api/app.py</code> <pre><code>def create_app() -&gt; FastAPI:\n    \"\"\"Create and configure the FastAPI application.\"\"\"\n    app = FastAPI(\n        title=\"Cloud FPGA Orchestrator\",\n        description=(\n            \"REST API for submitting HDL designs and running jobs on remote\"\n            \" FPGAs.\"\n        ),\n        version=\"0.1.0\",\n        lifespan=lifespan,\n    )\n\n    app.include_router(health_router)\n    app.include_router(fpga_router)\n    app.include_router(jobs_router)\n    app.include_router(session_router)\n\n    return app\n</code></pre>"}]}